<% include partials/header %>

<h1>Hello Blogs!</h1>

<% blogs.forEach(blog => { %>
  <div>
    <h2><%= blog.title %></h2>
    <img src="<%= blog.image %>" alt="<%= blog.title %>">
  </div>
<% }) %>

<% include partials/footer %>